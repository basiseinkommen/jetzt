import React from 'react';
import { Link } from 'gatsby';

import Layout from '@common/Layout';
import { Container, Section } from '@components/global';

const Imprint = () => (
  <Layout>
    <Container>
      <Section>
        <h1>Impressum</h1>
        <p>Impressumstext hier</p>
      </Section>
    </Container>
  </Layout>
);

export default Imprint;
